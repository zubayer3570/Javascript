let variable1 = 'dynamic, and here you have first example'
let variable2 = 'there you have another one'
let string = `this is a string
but this has some ability
which normal double/single quoted strings doesn't has
first one is, see with it you don't need to add line breaking to go into another line,
with it the string becomes ${variable1} ${variable2}`
console.log(string)